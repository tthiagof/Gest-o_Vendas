<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/Produtos/style.main.css">
    <title>Adicionar Produto</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-gray-200">
    <header class="w-full bg-gray-800 shadow p-4 flex justify-between items-center">
        <div class="flex items-center gap-3">
            <button onclick="toggleSidebar()" class="text-white text-2xl focus:outline-none">
                ☰
            </button>

            <h1 class="text-2xl text-white font-semibold">Sistema de Gestão</h1>
        </div>
        <div class="flex items-center gap-4">
            <nav class="flex gap-4 text-gray-400">
                <a href="#" class="hover:text-white">Configurações</a>
                <a href="#" class="hover:text-white">Perfil</a>
            </nav>
            <img src="/assets/profile.jpg" alt="Foto de Perfil"
                class="w-10 h-10 rounded-full object-cover border border-gray-600">
        </div>
    </header>

    <aside id="sidebar" class="fixed top-0 left-0 h-full w-64 bg-gray-900 text-gray-200
            transform -translate-x-full transition-transform duration-300 z-50">
        <div class="p-6 text-xl font-semibold border-b border-gray-700">
            Menu
        </div>

        <nav class="flex flex-col p-4 gap-4">
            <a href="#" class="hover:text-white">📦 Entrada de Produtos</a>
            <a href="#" class="hover:text-white">🚚 Saída de Produtos</a>
            <a href="#" class="hover:text-white">🔍 Consulta de Estoque</a>
            <a href="#" class="hover:text-white">🧾 Inventário</a>
            <a href="/dashboard/vendas" class="hover:text-white">💵 Vendas</a>
            <a href="#" class="hover:text-white">📜 Histórico de Vendas</a>
            <a href="/dashboard/addproduts" class="hover:text-white">🏷️ Produtos</a>
            <a href="/dashboard/gerenclientes" class="hover:text-white">👥 Clientes</a>
            <a href="#" class="hover:text-white">📊 Relatórios</a>
            <a href="#" class="hover:text-white">⚙️ Configurações</a>
        </nav>
    </aside>

    <div id="overlay" onclick="toggleSidebar()" class="fixed inset-0 bg-black bg-opacity-50 hidden z-40">
    </div>

    <main id="mainContent" class="p-6 transition-all duration-300">
        <div class="max-w-6xl mx-auto">

            <div class="flex justify-between items-center mb-8">
                <h2 class="text-3xl font-semibold">
                    Gerenciamento de Produtos
                </h2>

                <button onclick="toggleAddForm()"
                    class="bg-blue-600 hover:bg-blue-700 px-5 py-2 rounded-xl font-semibold shadow">
                    ➕ Adicionar Produto
                </button>
            </div>

            <section id="addForm" class="hidden bg-gray-800 p-6 rounded-2xl shadow mb-10">
                <h3 class="text-xl font-semibold mb-6 text-center">
                    Novo Produto
                </h3>

                <form method="POST" action="/dashboard/addproduts/add" class="grid grid-cols-1 md:grid-cols-3 gap-5">

                    <input name="nome" placeholder="Nome do Produto"
                        class="p-3 rounded bg-gray-900 border border-gray-700">

                    <input name="preco" type="number" step="0.01" placeholder="Preço (R$)"
                        class="p-3 rounded bg-gray-900 border border-gray-700">

                    <input name="quantidade" type="number" placeholder="Quantidade"
                        class="p-3 rounded bg-gray-900 border border-gray-700">

                    <button class="md:col-span-3 bg-blue-600 hover:bg-blue-700 py-3 rounded-xl font-semibold">
                        Salvar Produto
                    </button>

                    <% if (mensagem) { %>
                        <p class="md:col-span-3 text-center text-green-500 font-medium">
                            <%= mensagem %>
                        </p>
                        <% } %>
                </form>
            </section>

            <section class="grid gap-4">

                <% if (produtos && produtos.length> 0) { %>
                    <% produtos.forEach(produto=> { %>

                        <div
                            class="bg-gray-800 p-5 rounded-xl shadow flex flex-col md:flex-row md:items-center md:justify-between gap-4">

                            <div>
                                <p class="text-lg font-semibold">
                                    <%= produto.nome %>
                                        <% if (!produto.ativo) { %>
                                            <span class="text-red-500 text-sm ml-2">(Inativo)</span>
                                            <% } %>
                                </p>

                                <p class="text-sm text-gray-400">
                                    ID:<%= produto.id %> | Preço:<%=produto.preco %> | Quantidade:<%= produto.quantidade
                                                %>
                                </p>
                            </div>

                            <button onclick="toggleEdit('<%= produto.id %>')"
                                class="bg-yellow-600 hover:bg-yellow-700 px-4 py-2 rounded-lg font-medium">
                                ✏️ Editar
                            </button>
                        </div>

                        <div id="edit-<%= produto.id %>"
                            class="hidden bg-gray-900 p-6 rounded-xl border border-gray-700">

                            <form method="POST" action="/dashboard/addproduts/update/<%= produto.id %>"
                                class="grid grid-cols-1 md:grid-cols-3 gap-4">

                                <input type="hidden" name="id" value="<%= produto.produtID %>">

                                <div class="flex flex-col gap-1">
                                    <label class="text-sm font-medium text-gray-300">
                                        Nome
                                    </label>
                                    <input name="nome" value="<%= produto.nome %>"
                                        class="p-3 rounded bg-gray-800 border border-gray-700 text-gray-100 focus:outline-none focus:ring-2 focus:ring-yellow-600">
                                </div>

                                <div class="flex flex-col gap-1">
                                    <label class="text-sm font-medium text-gray-300">
                                        Preço
                                    </label>
                                    <input name="preco" value="<%= produto.preco %>"
                                        class="p-3 rounded bg-gray-800 border border-gray-700 text-gray-100 focus:outline-none focus:ring-2 focus:ring-yellow-600">
                                </div>

                                <div class="flex flex-col gap-1">
                                    <label class="text-sm font-medium text-gray-300">
                                        Quantidade
                                    </label>
                                    <input name="quantidade" value="<%= produto.quantidade %>"
                                        class="p-3 rounded bg-gray-800 border border-gray-700 text-gray-100 focus:outline-none focus:ring-2 focus:ring-yellow-600">
                                </div>

                                <div class="md:col-span-3 flex gap-3 mt-2">
                                    <button name="acao" value="salvar"
                                        class="bg-yellow-600 hover:bg-yellow-700 px-4 py-2 rounded font-semibold text-white-900 transition">
                                        Salvar Alterações
                                    </button>
                                    <% if (!produto.ativo) { %>
                                        <button type="submit"
                                            onclick="return confirm('Deseja realmente ATIVAR este produto?')"
                                            formaction="/dashboard/addproduts/activate/<%= produto.id %>"
                                            class="bg-green-600 hover:bg-green-700 px-4 py-2 rounded font-semibold text-white transition">
                                            Ativar Produto
                                        </button>
                                        <% } else { %>
                                            <button type="submit"
                                                onclick="return confirm('Deseja realmente INATIVAR este produto?')"
                                                formaction="/dashboard/addproduts/inactivate/<%= produto.id %>"
                                                class="bg-red-600 hover:bg-red-700 px-4 py-2 rounded font-semibold text-white transition">
                                                Inativar Produto
                                            </button>
                                            <% } %>
                                </div>
                            </form>

                        </div>

                        <% }) %>
                            <% } else { %>
                                <p class="text-center text-gray-400">
                                    Nenhum produto cadastrado.
                                </p>
                                <% } %>

            </section>

        </div>
    </main>
    <div id="loading-overlay">
        <div class="spinner"></div>
        <span style="font-family: sans-serif; font-weight: bold;">Processando...</span>
    </div>
    <script src="/Produtos/produtos.main.js"></script>
</body>

</html>